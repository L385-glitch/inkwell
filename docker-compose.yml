services:
  mynotes:
    build: .
    container_name: mynotes
    ports:
      # Reach via <truenas-tailscale-ip>:3100
      - "3100:3100"
    environment:
      - TZ=Europe/Vienna
    volumes:
      - ./data:/data
    restart: unless-stopped
